#pragma once
#include <iostream>

class Enemies
{
private:
	//Vec2 initialPos;
	//Rect rect;
	//EnemyType type;
	char overlapedSquare;
public:
	//void Move(Direction, Map);
	//void Hit(Map);
	void ReinitPos();
	void Reinit();
	//void Update(Direction, Map);
	void Draw();
};